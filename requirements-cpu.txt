# --------------------------------------------------------------
# Refactor (2026-04-18): Replaced Torch/Transformers with FastEmbed (MiniLM-L12-v2).
# This reduces install size from ~1GB to ~200MB and prevents system freezing.

# Core Embedding Engine (Ultra-Lite ONNX Runtime)
fastembed

# Parsing (Lightweight)
pdfplumber
sharepoint-to-text>=0.1.0

# UI & Storage
streamlit
lancedb
tantivy
pydantic
python-dotenv>=1.0.0
pyarrow

# Utilities
einops
split-words>=0.1.3
python-docx
